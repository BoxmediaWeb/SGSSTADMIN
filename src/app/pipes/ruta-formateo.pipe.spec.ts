import { RutaFormateoPipe } from './ruta-formateo.pipe';

describe('RutaFormateoPipe', () => {
  it('create an instance', () => {
    const pipe = new RutaFormateoPipe();
    expect(pipe).toBeTruthy();
  });
});
